import matrix.*

describe "Pruebas con la nave" {
  //Prueba 1
  test "La nave se choca" {
    nave.chocar()
    assert.equals(0, nave.cantPasajeros())
  }

  //Prueba 2
  test "Cuando la nave acelera, todos sus pasajeros, excepto el elegido, saltan de alegría y permanecen en la nave."{
    nave.acelerar()
    assert.equals(3, nave.cantPasajeros())
  }
}